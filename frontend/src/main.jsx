import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
const API=import.meta.env.VITE_API_URL||'/api/v1/todos';
function App(){const[todos,setTodos]=useState([]),[title,setTitle]=useState(''),[loading,setLoading]=useState(true),[error,setError]=useState('');
const load=async()=>{try{setLoading(true);const r=await fetch(API);if(!r.ok)throw Error();setTodos(await r.json());setError('')}catch{setError('Could not connect to the API')}finally{setLoading(false)}};useEffect(()=>{load()},[]);
const add=async e=>{e.preventDefault();if(!title.trim())return;const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title,completed:false})});if(r.ok){setTitle('');load()}else setError('Could not create todo')};
const toggle=async t=>{await fetch(`${API}/${t.id}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:t.title,completed:!t.completed})});load()};
const remove=async id=>{await fetch(`${API}/${id}`,{method:'DELETE'});load()};return <main><section className="card"><header><span className="eyebrow">PRODUCTIVITY</span><h1>Todo <em>today.</em></h1><p>Simple, fast, reliable task management.</p></header><form onSubmit={add}><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="What needs to be done?" maxLength="200"/><button>Add task</button></form>{error&&<div className="error">{error}</div>}{loading?<p className="muted">Loading…</p>:<ul>{todos.map(t=><li key={t.id} className={t.completed?'done':''}><button className="check" onClick={()=>toggle(t)} aria-label="Toggle task">{t.completed?'✓':'○'}</button><span>{t.title}</span><button className="delete" onClick={()=>remove(t.id)} aria-label="Delete task">×</button></li>)}</ul>}{!loading&&!todos.length&&<div className="empty">No tasks yet. Add your first one.</div>}</section></main>};createRoot(document.getElementById('root')).render(<App/>);